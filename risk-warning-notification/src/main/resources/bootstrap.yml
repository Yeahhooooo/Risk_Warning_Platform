spring:
  profiles:
    active: common
  application:
    name: risk-warning-notification
  cloud:
    nacos:
      discovery:
        # extra metadata for netty websocket port
        metadata:
            websocketPort: ${netty.websocket.port}
netty:
  websocket:
    port: 9090