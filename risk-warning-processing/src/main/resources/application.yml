server:
  port: 8084

assessment:
  reliability:
    enabled: true
    namespace: ${ASSESSMENT_WORK_NAMESPACE:${COMPUTERNAME:local}}-processing

# BERT 分类服务配置
bert:
  service:
    url: http://localhost:8090  # BERT 服务地址（Java 服务端口）

# Knowledge 向量化服务配置
knowledge:
  service:
    url: http://localhost:8089  # Knowledge 服务地址


spring:
  batch:
    job:
      enabled: false
